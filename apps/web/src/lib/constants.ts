/** Cookie httpOnly com a org ativa (workspace selecionado). */
export const ACTIVE_ORG_COOKIE = "wayline_active_org";

/** Cookie httpOnly com a lista ativa (board selecionado na sidebar). */
export const ACTIVE_LIST_COOKIE = "wayline_active_list";
